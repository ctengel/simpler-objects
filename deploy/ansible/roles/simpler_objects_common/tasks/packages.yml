---
- name: Install OS packages for simpler-objects
  ansible.builtin.apt:
    name:
      - python3
      - python3-venv
      - git
    state: present
    update_cache: true
    cache_valid_time: 3600
