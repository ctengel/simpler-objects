---
# Package source. Bump this to upgrade; pip detects the changed ref and
# reinstalls. Idempotent at the same tag.
simpler_objects_version: v0.4.2
simpler_objects_git_url: https://github.com/ctengel/simpler-objects

# Install layout. The systemd units use %h/venv as the default venv path;
# override with a drop-in (`systemctl --user edit <unit>`) if you change it.
simpler_objects_venv: "{{ ansible_env.HOME }}/venv"

# Config and unit directories (matches %E and %h/.config/systemd/user in unit files).
simpler_objects_config_dir: "{{ ansible_env.HOME }}/.config/simpler-objects"
simpler_objects_systemd_user_dir: "{{ ansible_env.HOME }}/.config/systemd/user"
