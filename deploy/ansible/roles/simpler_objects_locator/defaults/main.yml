---
locator_port: 29164
locator_workers: 1

# `locator_object_servers` must be set per-host in inventory — list of URLs
# (with trailing slashes; the locator concatenates paths directly).
# locator_object_servers: []
