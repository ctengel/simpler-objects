{{ ansible_managed | comment }}
# Env file for simpler-objects-object-server.service.
# Generated by Ansible — edits will be overwritten on the next playbook run.

OBJECT_DIRECTORY={{ object_directory }}
READ_ONLY={{ '1' if (object_server_read_only | default(false)) else '' }}
WORKERS={{ object_server_workers | default(object_server_default_workers) }}
PORT={{ object_server_port | default(object_server_default_port) }}
