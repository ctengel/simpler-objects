{{ ansible_managed | comment }}
# Per-instance env file for simpler-objects-object-server@{{ instance.name }}.service.
# Generated by Ansible — edits will be overwritten on the next playbook run.

OBJECT_DIRECTORY={{ instance.object_directory }}
READ_ONLY={{ '1' if (instance.read_only | default(object_server_default_read_only)) else '' }}
WORKERS={{ instance.workers | default(object_server_default_workers) }}
PORT={{ instance.port | default(object_server_default_port) }}
